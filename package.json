{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "release": "turbo run build lint --filter=@trout.run/cli && changeset version && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "@turbo/gen": "^1.10.7",
    "eslint": "^8.44.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^3.0.0",
    "turbo": "^1.10.7"
  },
  "packageManager": "pnpm@8.6.6",
  "name": "trout"
}

{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "release": "turbo run --filter=@trout.run/cli build lint && changeset version && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "@turbo/gen": "^1.10.12",
    "eslint": "^8.46.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^3.0.0",
    "turbo": "^1.10.12"
  },
  "packageManager": "pnpm@8.6.7",
  "name": "trout"
}
